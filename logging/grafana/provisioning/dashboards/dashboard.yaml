apiVersion: 1

providers:
  - name: 'default'
    folder: 'FastAPI Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
