# 🚀 Запуск проекта

### 1. Установка Docker

- Скачайте и установите [Docker](https://www.docker.com/).

### 2. Клонирование репозитория

### 3. Корректировка `.sh`-файлов (только для Windows)

- В файлах с расширением `.sh` замените разделители строк с **CRLF** на **LF**.

- В PyCharm: нажмите **CRLF** в правом нижнем углу → выберите **LF**.

### 4. Настройка переменных окружения через Infisical

1. Перейдите на сайт [Infisical](https://app.infisical.com/organization/projects).

2. Создайте два новых файла окружения:

    - `.env` (на основе `.env.example`)

    - `.env.app_config` (на основе `.env.app_config.example`)

3. Поместите их в те же директории, где лежат их шаблоны.

4. Запишите все переменные из infisical в нужные .env файлы

### 5. Создание PEM-файлов

1. Через Infisical найдите переменные:

    - `JWT-PRIVATE`

    - `JWT-PUBLIC`

2. Сохраните их содержимое в файлы:

    - `jwt-private.pem`

    - `jwt-public.pem`

### 6. Запуск проекта

В корневой папке проекта выполните:

```bash
docker compose --env-file src/backend/.env up -d --build
```

---

# 📊 Просмотр базы данных

### 1. Запустите проект (см. инструкцию выше)

### 2. Установите DBeaver

- Скачать можно с [официального сайта](https://dbeaver.io/download/).

### 3. Подключение к PostgreSQL

1. Нажмите иконку **розетки** в левом верхнем углу.

2. Выберите **PostgreSQL**.

3. Введите параметры подключения из `.env` файла:

    - Host

    - Port

    - Database

    - Username

    - Password

4. Поставьте галочку **"Показывать все базы данных"**.

5. В разделе **"Advanced"** включите все чекбоксы, **кроме** `Replace legacy timezone`.

### 4. Работа с базой данных

- Перейдите в: `Базы данных → DB_project → Схемы → public`.

- Откройте диаграмму `public`.

- Дважды кликая по таблицам, можно просматривать их содержимое.

---

# 📈 Просмотр логов через Grafana

### 1. Откройте Grafana

- Перейдите по адресу: [http://localhost:3000](http://localhost:3000/)

### 2. Войдите в систему

- Логин: `admin`

- Пароль: `admin`

### 3. Откройте дашборд

- Перейдите на вкладку **Dashboards**.

- Откройте доступный дашборд для просмотра логов.

# 🔄 Обновление `.env` файлов

### 1. Обновите `.env` локально

- Добавьте новую переменную в ваш локальный `.env` файл.

### 2. Синхронизируйте с `.env.example`

- Обязательно добавьте ту же переменную (с примерным значением или пустым) в файл `.env.example`, чтобы другие
  разработчики знали о её наличии.

### 3. Добавьте переменную в Infisical

1. Перейдите на [Infisical](https://app.infisical.com/organization/projects).

2. Откройте нужный проект и выберите соответствующий `.env` файл.

3. Добавьте новую переменную для файлов .env.app_config и .env  **с нужными тегами** `appconfig`, `dockersetup`
   соответственно для каждого файла.

4. Убедитесь, что переменная корректно сохранилась и видна другим участникам проекта.
